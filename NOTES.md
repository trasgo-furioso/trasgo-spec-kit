# Notes

- the persona is a team - that could be solo team - the workflow phases demands skills not roles. this is how we keep trasgospec agnostic to team size, that is the same to say escales.
- build on top of product-manager speckit bundle
- web-based markdown editor that uses the filesystem as persistance backend and git as audit and versioning. it allows users to access update and read the docs easier than in IDE.
- plan, tasks and implement done by async agents - i currently pass the orchestration instructions to the implement command
- tailor implementation with specialized agents
- quickstart.md could be used for knowing what to e2e test after implementation to validate the user journey from the user perspective, that's the final integration test.