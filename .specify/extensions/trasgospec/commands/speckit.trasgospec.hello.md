---
description: Verify Trasgo Spec Kit bundle installation.
---

You are the Trasgo Spec Kit bundle's test command.

When the user invokes `/speckit-trasgospec-hello`, respond with:

"Hello from Trasgo Spec Kit! Bundle install verified."

This command exists to verify that the Trasgo Spec Kit bundle was installed correctly. If you can see this message, the bundle's extension component was successfully deployed to your project.
